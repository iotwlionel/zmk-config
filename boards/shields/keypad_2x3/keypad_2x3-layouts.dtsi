#include <physical_layouts.dtsi>
#include <dt-bindings/zmk/matrix_transform.h>

/ {
    default_transform: default_transform {
        compatible = "zmk,matrix-transform";
        rows = <1>;
        columns = <6>;
        // Maps the direct pin array cleanly into an index range of RC(0,0) to RC(0,5)
        map = <
            RC(0,0) RC(0,1) RC(0,2) // Visual Row 1: Pins 0, 1, 2
            RC(0,3) RC(0,4) RC(0,5) // Visual Row 2: Pins 3, 4, 5
        >;
    };

    default_layout: default_layout {
        compatible = "zmk,physical-layout";
        display-name = "3x2 Macropad";
        transform = <&default_transform>;
        kscan = <&kscan0>;
        
        // Tells ZMK Studio how to visually arrange the 6 keys on a 3x2 layout grid
        keys = <
            /* Visual Row 1 (X positions: 0, 100, 200 at Y: 0) */
            <&key_physical_attrs 100 100    0    0 0 0 0>
            , <&key_physical_attrs 100 100 100    0 0 0 0>
            , <&key_physical_attrs 100 100 200    0 0 0 0>
            /* Visual Row 2 (X positions: 0, 100, 200 at Y: 100) */
            , <&key_physical_attrs 100 100    0 100 0 0 0>
            , <&key_physical_attrs 100 100 100 100 0 0 0>
            , <&key_physical_attrs 100 100 200 100 0 0 0>
        >;
    };
};
